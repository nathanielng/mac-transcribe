[project]
name = "mac-transcribe-pipeline"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mlx-whisper>=0.4.0",
    "anthropic>=0.40.0",
]

[project.optional-dependencies]
cjk = ["mlx-audio>=0.1.0"]

[tool.uv]
package = false
